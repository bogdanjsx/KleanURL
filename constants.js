// Use this config if, for whatever reason, getting from storage yields
// an invalid object.
const INITIAL_CONFIG = {isActive: true, rules: []},
      EXTENSION_NAME = 'repLink',
      STORAGE_KEY = EXTENSION_NAME + 'Config',
      EDIT_TEXT = 'Edit',
      DELETE_TEXT = 'Delete',
      CONFIRM_DELETE_TEXT = 'I am sure';

